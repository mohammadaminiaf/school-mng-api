meta {
  name: Create student
  type: http
  seq: 2
}

post {
  url: {{URL}}/students
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "full_name": "Chandler",
    "father_name": "Father Name",
    "gender": "male"
  }
}
