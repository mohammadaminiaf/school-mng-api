meta {
  name: Create staff role
  type: http
  seq: 2
}

post {
  url: {{URL}}/staff-roles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "cleaner"
  }
}

settings {
  encodeUrl: true
}
